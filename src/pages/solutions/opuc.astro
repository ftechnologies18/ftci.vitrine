---
import ProductLayout from '../../components/ProductLayout.astro';
import { getProduct } from '../../data/products';

const product = getProduct('opuc');
if (!product) throw new Error('Product "opuc" not found');
---

<ProductLayout product={product} />
