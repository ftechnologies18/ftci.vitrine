---
// About section - Company positioning and mission
// Cahier des charges 6.2: compteurs animés au scroll
// Skills: NYT Data Viz (tabular-nums, ONE hero accent, source lines), Apple Design (reduced motion = instant)
---

<section id="about" class="py-20 md:py-32 bg-white relative overflow-hidden">
	<!-- Background Pattern (cahier des charges 6.4: motif circuit) -->
	<div
		class="absolute inset-0 -z-10 opacity-[0.05]"
		style={{
			backgroundImage: 'url(/manus-storage/ftci-tech-grid_9a960dd7.png)',
			backgroundSize: '200px 200px',
		}}
	></div>

	<div class="container">
		<div class="grid md:grid-cols-2 gap-12 items-center">
			<!-- Left Content -->
			<div class="observe">
				<h2 class="text-4xl md:text-5xl font-bold text-navy mb-6">
					À Propos de FTCI
				</h2>

				<p class="text-lg text-gray-text mb-6 leading-relaxed">
					Freelance Technologies Côte d'Ivoire est un cabinet de conseil en transformation digitale et éditeur de solutions logicielles SaaS pensées pour le marché ivoirien et africain.
				</p>

				<p class="text-lg text-gray-text mb-6 leading-relaxed">
					Depuis notre création, nous accompagnons universités, entreprises et institutions dans leur digitalisation, du conseil stratégique jusqu'au déploiement d'outils numériques sur mesure.
				</p>

				<div class="space-y-4 mb-8">
					<div class="flex items-start gap-4">
						<div class="w-8 h-8 bg-gradient-to-br from-orange to-orange-600 rounded-lg flex items-center justify-center text-white flex-shrink-0 mt-1">
							<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
							</svg>
						</div>
						<div>
							<h4 class="font-semibold text-navy mb-1">Expertise Locale</h4>
							<p class="text-gray-text">Comprendre les défis spécifiques des institutions africaines</p>
						</div>
					</div>

					<div class="flex items-start gap-4">
						<div class="w-8 h-8 bg-gradient-to-br from-green to-green-600 rounded-lg flex items-center justify-center text-white flex-shrink-0 mt-1">
							<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
							</svg>
						</div>
						<div>
							<h4 class="font-semibold text-navy mb-1">Excellence Technique</h4>
							<p class="text-gray-text">Solutions modernes, performantes et sécurisées</p>
						</div>
					</div>

					<div class="flex items-start gap-4">
						<div class="w-8 h-8 bg-gradient-to-br from-periwinkle to-blue-600 rounded-lg flex items-center justify-center text-white flex-shrink-0 mt-1">
							<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
							</svg>
						</div>
						<div>
							<h4 class="font-semibold text-navy mb-1">Partenariat Durable</h4>
							<p class="text-gray-text">Support continu et évolution selon vos besoins</p>
						</div>
					</div>
				</div>

				<a
					href="#contact"
					class="magnetic inline-flex items-center gap-2 px-6 py-3 border-2 border-navy text-navy rounded-lg font-semibold hover:bg-navy/5 active:scale-95 transition-all duration-200"
					data-magnetic
				>
					En savoir plus
					<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
					</svg>
				</a>
			</div>

			<!-- Right: Stats (NYT Data Viz discipline) -->
			<div class="observe">
				<div class="bg-light-bg rounded-2xl p-8 md:p-12">
					<!-- HERO STAT: 100K+ users (the most impressive — ONE accent) -->
					<div class="text-center mb-10 pb-8 border-b border-gray-200">
						<div class="text-5xl md:text-6xl font-bold text-orange mb-2 stat-value">
							<span class="counter" data-target="100">0</span>K+
						</div>
						<p class="text-navy font-semibold text-lg">Utilisateurs actifs</p>
						<p class="text-gray-text text-xs italic mt-1">Source: FTCI — agrégat utilisateurs SaaS, 2026</p>
					</div>

					<!-- Secondary stats (3, de-emphasized, tabular-nums) -->
					<div class="grid grid-cols-3 gap-6">
						<!-- Stat: Solutions -->
						<div class="text-center">
							<div class="text-3xl md:text-4xl font-bold text-navy mb-1 stat-value">
								<span class="counter" data-target="4">0</span>
							</div>
							<p class="text-gray-text text-sm font-medium">Solutions SaaS</p>
							<p class="text-gray-text text-[10px] italic mt-0.5">SECT, OPUC, CATS, ScolaGest</p>
						</div>

						<!-- Stat: Institutions -->
						<div class="text-center">
							<div class="text-3xl md:text-4xl font-bold text-green mb-1 stat-value">
								<span class="counter" data-target="50">0</span>+
							</div>
							<p class="text-gray-text text-sm font-medium">Institutions servies</p>
							<p class="text-gray-text text-[10px] italic mt-0.5">Universités & écoles, CI</p>
						</div>

						<!-- Stat: Satisfaction -->
						<div class="text-center">
							<div class="text-3xl md:text-4xl font-bold text-periwinkle mb-1 stat-value">
								<span class="counter" data-target="99">0</span>%
							</div>
							<p class="text-gray-text text-sm font-medium">Satisfaction client</p>
							<p class="text-gray-text text-[10px] italic mt-0.5">Enquête interne, 2026</p>
						</div>
					</div>

					<!-- Location -->
					<div class="mt-10 pt-8 border-t border-gray-200">
						<p class="text-sm text-gray-text mb-4 font-medium tracking-wide">BASÉE EN CÔTE D'IVOIRE</p>
						<p class="text-navy font-semibold mb-2">Abidjan & Dabou</p>
						<p class="text-gray-text text-sm">
							Nous accompagnons les institutions africaines dans leur transformation digitale, avec une expertise locale et une excellence technique.
						</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<script>
	// === Animated counters (cahier des charges 6.2) ===
	// NYT Data Viz: tabular-nums prevents jitter during count-up (set in CSS .stat-value)
	// Apple Design: reduced-motion = instant final value (no vestibular trigger)
	const counters = document.querySelectorAll('.counter');
	const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	const runCounter = (counter: Element) => {
		const target = parseInt(counter.getAttribute('data-target') || '0');

		// Reduced motion: snap to final value immediately
		if (prefersReducedMotion) {
			counter.textContent = String(target);
			return;
		}

		const duration = 1800; // 1.8s — under 2s, snappy enough
		const startTime = performance.now();

		const updateCounter = (now: number) => {
			const elapsed = now - startTime;
			const progress = Math.min(elapsed / duration, 1);
			// ease-out curve for natural deceleration
			const eased = 1 - Math.pow(1 - progress, 3);
			const current = Math.floor(eased * target);
			counter.textContent = String(current);

			if (progress < 1) {
				requestAnimationFrame(updateCounter);
			} else {
				counter.textContent = String(target);
			}
		};

		requestAnimationFrame(updateCounter);
	};

	// Trigger on scroll into view
	const observer = new IntersectionObserver(
		(entries) => {
			entries.forEach((entry) => {
				if (entry.isIntersecting) {
					runCounter(entry.target);
					observer.unobserve(entry.target);
				}
			});
		},
		{ threshold: 0.5 }
	);

	counters.forEach((counter) => observer.observe(counter));
</script>

<style>
	.observe {
		opacity: 0;
		transform: translateY(30px);
		transition: opacity 0.6s var(--ease-out), transform 0.6s var(--ease-out);
	}

	.observe.is-visible {
		opacity: 1;
		transform: translateY(0);
	}

	@media (prefers-reduced-motion: reduce) {
		.observe {
			opacity: 1;
			transform: none;
			transition: opacity 0.2s ease;
		}
	}
</style>
